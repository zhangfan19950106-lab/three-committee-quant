# 📖 Memory Map — 分层索引

> 每日启动只读重要的文件。按需求情境选择性加载。
> 文件 = 按需加载。日常只加载「当前必须读」的部分。

## 🚫 L0 — 系统红线（每次必读）

**文件：** `MEMORY.md`（L0 + L1 部分）
**加载条件：** 每次启动必读
**原因：** 违反会产生实际损失

## ⚙️ L1 — 业务规则（每次必读）

同上，在 MEMORY.md 中。每次启动必读。

## 📚 L2 — 知识/配置（按需加载）

| 文件 | 内容 | 加载条件 |
|:----|:----|:---------|
| `memory/position.md` | 当前持仓（成本/盈亏/终裁） | 做交易分析时 |
| `memory/tools.md` | CLI路径、API Key、脚本参数 | 执行脚本/调试时 |
| `memory/issues.md` | 已知Bug、约束、待解决 | 写新脚本/修Bug时 |
| `memory/YYYY-MM-DD.md` | 当日日志 | 查找近期上下文时 |
| `memory/lessons.md` | 教训总结（学到的经验） | 写新分析脚本时 |

## 🧠 会话记忆（日常不主动加载）

- 已压缩的 LCM 摘要：系统自动管理，按需展开
- session-memory hook：`/new` 时自动归档最后15条

## 启动加载规则

每次启动：
1. **始终加载：** MEMORY.md（仅 L0+L1 部分），SOUL.md，IDENTITY.md，USER.md
2. **当前任务相关：** 判断用户在聊什么 → 加载对应 L2 文件
3. **不加载：** 除当日外的 daily logs，LCM 摘要（系统自动管）
